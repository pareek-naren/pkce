{
  "name": "oauth_code_challenge_generator",
  "version": "1.0.0",
  "description": "Generates the code challenge from the code verifier",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "crypto": "^1.0.1"
  }
}
